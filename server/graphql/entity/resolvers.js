/* ------ Global Dependencies ------ */

/* ------ Local Dependencies ------ */

/* ------ Resolver Functions ------ */
const resolveFunctions = {
  Query: {
    async entityRetrieve (_, input, context) {

    },
    async entityList (_, input, context) {

    }
  },
  Mutation: {
    async entityCreate (_, input, context) {

    },
    async entityUpdate (_, input, context) {

    },
    async entityDelete (_, input, context) {

    },
  }
}

export default resolveFunctions
